node_modules
.next
out
.env
.env.local
*.log
.DS_Store
coverage

# TypeScript incremental build cache — generated artifact, not source
tsconfig.tsbuildinfo

# Newvora agency onboarding forms — business docs, not part of this app
Newvora-Form-*.md

# Private handoff/context doc — never commit to the public repo
MASTER_CONTEXT_HANDOFF.md
