// Legacy /wholesale route group — kept only to 308-redirect old dealer bookmarks
// to the new isolated /trade portal. No retail chrome, no shared layout.
export default function LegacyWholesaleLayout({ children }: { children: React.ReactNode }) {
  return children;
}
